# Trackpad DJ 검증 기록

## 2026-09-07 반복 조그 이동 무시 후속 수정

- 두 번째 손가락만 움직이면 스크래치 명령이 나오지 않는 기존 경로를 테스트 FAIL로 재현하고 수정 후 PASS를 확인했다. 시작한 두 손가락 중 움직이는 손가락들의 속도를 평균하며 세 번째 터치는 여전히 무시한다.
- 매 터치 콜백의 전체 `.touching` 목록으로 상태를 갱신해 누락된 종료를 복구한다. 동일 이벤트 중복 전달과 한 손가락을 남긴 채 다른 손가락을 100회 교체하는 입력을 테스트한다.
- [Apple 터치 이벤트 문서](https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/EventOverview/HandlingTouchEvents/HandlingTouchEvents.html)의 resting 상태 전환 동작에 따라 `wantsRestingTouches`를 활성화해 물리 접촉을 계속 수신한다.
- 실제 사용자 현상의 이벤트 로그는 확보하지 않았으므로 원인 전체를 확정하지 않는다. 반복 조작의 물리 입력과 오디오 청취는 사용자 확인이 필요하다.
- `swift build`, `swift test` (60 tests, 0 failures), `Scripts/verify-strict-concurrency.sh`, `git diff --check`: PASS.

## 2026-09-07 두 손가락 조그 및 스크래치 복귀 수정

- 한 손가락은 조그를 시작하지 않고, 두 번째 손가락 인식 시 덱·모드를 고정한다. 한 손가락만 남으면 해제하며 다시 두 손가락이 되면 새 조그를 시작한다.
- 스크래치 중 곡 끝에 닿아도 재생 상태를 끄지 않는다. 역방향으로 돌아온 뒤 해제하면 기존 재생·정지 상태를 유지한다. 일반 재생의 곡 끝 정지는 유지한다.
- `swift test`: 58 tests, 0 failures. 한 손가락 이동 무시, 두 번째 손가락 해제·재시작, 곡 끝 스크래치 후 역방향 복귀 회귀 테스트를 포함한다.
- `swift build`, `Scripts/verify-strict-concurrency.sh`, `git diff --check`: PASS.
- 사용자가 보고한 멈춤 전체가 동일 원인인지는 미확인이다. 실제 두 손가락 조작성과 스크래치 후 소리 복귀는 실기기 재검증이 필요하다.

## 단일 조그·퍼포먼스 콘솔 개편 결과

2026-08-21 `main`의 PR #5 병합 결과 기준이다. 빌드와 결정론적 자동 검증은 현재 소스에서 다시 PASS했다. 기본 크기 실제 앱 화면 관찰과 현재 힙합 2곡의 자동 BPM 표시는 2026-08-19 결과를 유지하며, 다양한 장르와 첫 박자 정확도, 물리 트랙패드 조작성, 화면 버튼의 물리 클릭, 창 수동 리사이즈와 오디오 청취 품질은 `UNVERIFIED`다.

| 구분 | 결과 | 증거와 범위 |
|---|---|---|
| `swift build` | PASS | Debug 실행 파일 빌드 |
| `swift test` | PASS | 56 tests, 0 failures |
| `Scripts/verify-strict-concurrency.sh` | PASS | `-strict-concurrency=complete -warnings-as-errors` 실행 타깃 빌드 |
| `git diff --check` | PASS | 공백 오류 없음 |
| 활성 덱·공용 키 | PASS | 기본 A, `Tab`, 공용 단발 키, 연속 키 시작 덱 고정, 전역 크로스페이더 독립 |
| 단일 조그 trace | PASS | 전체 표면, 수평 무시, 첫 터치, 시작 덱·모드 고정, 멀티터치 비승격, 50ms 정지와 취소 |
| 피치 벤드 렌더 상태 | PASS | ±8% 클램프, 템포 합산, 스크래치 우선순위, 정지 덱 재생 위치 불변 |
| 프리페이더 피크 | PASS | 실제 다중 채널 렌더 최대 절댓값, 동시 atomic max와 snapshot consume-reset |
| 자동 BeatGrid | PASS | 90/120/150 BPM 합성 클릭, 밀집 에너지 변조, 기본/더블 템포 결합, 선행 무음, 짧은 신호와 저신뢰 신호 거부 |
| 실제 음악 자동 BPM | 제한적 PASS | `Jazz (We've Got)` 92.8 BPM/AUTO와 `A Tribe Called Jazzyfact` 95.8 BPM/AUTO를 새 바이너리 UI에서 확인. 전자는 파일의 92 BPM 태그와 일치하며 다양한 장르와 첫 박자 청취 검증은 제외 |
| 탭 BPM·원샷 SYNC | PASS | 두 번째 탭부터 덮어쓰기, 자동값 복원, 상대 템포 반영, BPM 누락·±8% 초과 시 무변경 |
| 커서 잠금 상태 | PASS | 가짜 시스템 호출로 멱등성, 실패 롤백, `hide`/`unhide` 균형 확인 |
| 비동기 로드 | PASS | 실패 시 기존 트랙 유지, 이전 요청 지연 완료 무시, 새 트랙의 탭·템포·벤드 초기화 |
| 3단 출력 게이트 / Split Cue 회귀 | PASS | A만 `(1, 0)`, 중앙 `(1, 1)`, B만 `(0, 1)`과 기존 합성 신호·그래프 테스트 유지 |
| 반응형 UI 계약 | PASS | 960×620, 1180×720, 1600×900 레이아웃 범위·비중첩, hit test, 활성 정책과 offscreen 렌더 |
| 화면 컨트롤 명령 | PASS | 비활성 덱 버튼의 `selectActiveDeck` 선행, 전역 출력 버튼의 덱 불변, 빈 덱 명령 비활성화 |
| 버튼/조그 라우팅 | PASS | 첫 터치·커서 잠금·컨트롤 존재 조합을 순수 정책 테스트로 확인 |
| 콘솔 렌더링 책임 분리 | PASS | `TouchLabView`의 입력·수명주기와 불변 상태 기반 `PerformanceConsoleRenderer`를 분리하고 렌더러 직접 offscreen 테스트 통과 |
| 3단 크로스페이더 UI | PASS | 실제 1180×720 앱 창에서 기본 `A+B ON`, 왼쪽 `A ON`, 오른쪽 `B ON` 강조 전환 확인 |
| 새 데스크톱 UI 관찰 | PASS | 실제 1180×720 앱 창에서 빈 덱과 두 트랙 로드 상태, 세로 파형, 단일 플래터, 덱 콘솔, 피크 미터와 `Tab` A/B 강조 전환 확인 |
| 화면 버튼 물리 클릭 | UNVERIFIED | 그린 영역과 hit test 자동 검증만 수행, 실제 손가락 클릭과 조그 억제는 수행하지 않음 |
| 창 수동 리사이즈 | UNVERIFIED | 최소·기본·큰 크기 계산과 offscreen 렌더는 PASS, 실제 창 드래그는 수행하지 않음 |
| 실제 macOS 커서 결합 | UNVERIFIED | Core Graphics 호출은 빌드 및 가짜 호출로만 검증, 실제 `P` 동작은 수행하지 않음 |

WAV fixture 로드 중 AVFAudio가 비인터리브 설정을 무시한다는 진단 메시지가 출력되지만 테스트와 디코딩은 성공한다. 이를 실제 장치 음질이나 위 두 곡을 넘어선 실제 음악 BPM 정확도의 증거로 사용하지 않는다.

## 실기기 체크리스트

기본·로드 화면과 `Tab` 전환 표시는 관찰했지만, 아래 물리 입력과 오디오 동작 대부분은 수행하지 못해 `UNVERIFIED`다.

- [x] 1180×720 기본 창에서 세로 파형, 좌우 덱 콘솔과 중앙 단일 플래터가 겹치지 않음
- [x] 두 트랙 로드 상태에서 BPM·남은 시간·파형과 프리페이더 미터가 표시됨
- [x] 활성 덱 강조, 플래터 문자·색과 `JOG A/B / SCRATCH`가 `Tab` 전환을 함께 반영
- [ ] 960×620 최소 크기와 큰 창 사이 수동 리사이즈에서 잘림·겹침·hit target 오류가 없음
- [ ] `LOAD`, `CUE`, `PLAY/PAUSE`, `SYNC`, `MON`, 덱 선택과 출력 버튼을 실제 포인터 클릭으로 반복 조작
- [ ] 커서 잠금 해제 상태의 버튼 위 첫 터치가 클릭만 수행하고 조그를 시작하지 않음
- [ ] 버튼 밖에서 시작한 터치와 커서 잠금 중 터치는 전체 표면 조그로 동작
- [ ] 기본 JOG A에서 전체 트랙패드가 Deck A만 스크래치
- [ ] `Tab` 후 다음 터치가 Deck B만 제어하고, 진행 중 터치는 시작 덱에 유지
- [ ] 일반 스크래치와 `Shift` 시작 피치 벤드의 방향·감도·50ms 정지·종료 복귀
- [ ] 피치 벤드 중 클릭 노이즈가 없고 정지 덱 재생 위치가 유지
- [ ] 수평·대각선 이동이 볼륨, 필터나 크로스페이더를 변경하지 않음
- [ ] `P` 잠금·해제와 `Esc`, 파일 선택, 포커스 상실, 창 닫기 후 커서 복구
- [ ] 실제 조그 중 `BEND`, `NEXT`와 `CURSOR LOCKED` 표시
- [x] 현재 힙합 2곡을 새 바이너리에 다시 로드했을 때 92.8/95.8 BPM과 `AUTO`가 표시됨
- [ ] 다양한 장르의 자동 BPM·첫 박자 정확도와 분석 실패 표시
- [ ] `B` 탭 보정, `Shift+B` 자동 복원과 새 트랙 로드 후 초기화
- [ ] `S` 원샷 SYNC 뒤 템포만 맞고 위상은 수동 조그로 조절 가능
- [ ] 실제 음악 두 곡 로드 → 큐 → 재생 → SYNC/템포 → 볼륨·필터 → 크로스페이드
- [ ] 헤드폰 왼쪽에서 Master만, 오른쪽에서 `V`로 선택한 Cue만 청취
- [ ] 프리큐 토글 중 왼쪽 Master 음량 불변과 두 덱 동시 프리큐 헤드룸
- [ ] 헤드폰 또는 출력 장치 연결·해제 후 안전한 복구와 프리큐 무유출
- [ ] 30분 연속 믹싱 중 클릭, 멈춤, 잘못된 키 고정과 상태 누적 없음

실기기 검증 시 사용한 맥북·트랙패드, 오디오 장치, macOS 버전, 트랙 형식·샘플레이트와 관찰 결과를 함께 기록한다. 빌드 성공이나 합성 신호 PASS만으로 이 체크박스를 완료 처리하지 않는다.

## 작업 전 기준선

단일 조그 개편을 시작한 2026-08-19의 깨끗한 작업 트리 기준이다.

| 확인 | 결과 | 당시 증거 |
|---|---|---|
| `swift test` | PASS | 31 tests, 0 failures |
| `Scripts/verify-strict-concurrency.sh` | PASS | 엄격 동시성 실행 타깃 빌드 |
| 입력 계약 | 존 기반 | 상단 볼륨, 좌우 덱 스크래치·필터, 하단 연속 크로스페이더 |
| BPM | 부분 구현 | 수동 탭과 표시만 제공 |
| 커서 잠금 | 미구현 | 조그와 커서 이동이 항상 결합 |

단일 조그·퍼포먼스 콘솔 개편은 GitHub PR #1로 `main`에 병합했다. 입력 뷰와 콘솔 렌더러의 책임 분리는 GitHub PR #5로 `main`에 병합했으며 사용자 입력·오디오·화면 배치는 변경하지 않았다. 실제 앱 화면 관찰 과정에서 로컬 음악 파일은 읽기 전용으로 로드했으며 원본 파일은 변경하지 않았다.
