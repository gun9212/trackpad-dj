# Trackpad DJ Project Guide

이 문서는 Trackpad DJ 저장소의 제품 방향, 현재 구현 상태, 입력 규칙, 아키텍처, 개발 우선순위와 작업 규칙을 관리하는 기준 문서다.

에이전트는 작업을 시작하기 전에 이 문서와 변경 대상 소스를 읽어야 한다. 문서의 목표와 현재 소스가 다르면 추측으로 맞추지 말고 차이를 명시한다. 의미 있는 기능 변경으로 프로젝트 상태가 달라졌다면 이 문서의 `현재 구현 상태`와 `다음 우선순위`도 함께 갱신한다.

## 0. 사용자 응답 형식

- 사용자에게 보이는 자연어 `commentary`와 `final` 응답은 첫 줄에 `◆ CODEX ━━━━━━━━━━━━━━━━━━━━━`를 단독으로 표시한다.
- 구분선 다음에는 빈 줄을 하나 둔 뒤 본문을 작성한다.
- 코드 블록, 명령어, 파일 내용, 인용문과 사용자가 요청한 정확한 출력 형식 안에는 구분선을 삽입하지 않는다.
- 사용자가 헤더 생략이나 정확한 기계 판독 형식을 명시적으로 요구하면 해당 요청을 우선한다.
- 별도 요청이 없다면 사용자 응답과 인수인계 문서는 한국어로 작성한다.

## 1. 프로젝트 정의

Trackpad DJ는 **맥북의 내장 트랙패드와 키보드를 함께 사용해 실제로 간단한 디제잉을 할 수 있는지 검증하는 macOS 우선 실험 앱**이다.

핵심 목표는 다음과 같다.

- 두 곡을 각각 Deck A와 Deck B에 로드한다.
- 재생, 정지, 큐와 기본적인 위치 이동을 수행한다.
- 트랙패드 전체를 하나의 조그휠로 사용해 스크래치와 피치 벤드를 연속 제어한다.
- 키보드로 트랙 로드, 트랜스포트와 보조 제어를 빠르고 확실하게 수행한다.
- 외부 DJ 컨트롤러 없이 두 곡을 자연스럽게 전환한다.

이 프로젝트는 완성형 프로 DJ 장비를 복제하는 제품이 아니다. **트랙패드와 키보드만으로 플레이 가능한 2덱 믹싱 경험**을 만드는 HCI + 오디오 인터랙션 프로젝트다.

## 2. 제품 원칙

우선순위는 다음 순서를 따른다.

1. 두 덱의 안정적인 재생과 자연스러운 곡 전환
2. 공연 중 헷갈리지 않는 활성 덱과 단일 조그·키보드 매핑
3. 빠른 로드, 재생·정지, 큐와 위치 조절
4. 덱별 볼륨, 필터와 크로스페이더
5. 기본 스크래치의 반응성과 음질
6. BPM, 핫큐, 루프와 같은 확장 기능

트랙패드와 키보드의 역할은 다음과 같이 나눈다.

- **트랙패드:** 활성 덱 스크래치와 순간 피치 벤드를 위한 단일 세로 조그
- **키보드:** 활성 덱 선택, 로드, 트랜스포트, 믹서 파라미터, 탭 BPM과 원샷 SYNC
- **화면 컨트롤:** 현재 믹싱 상태를 읽고 활성 덱과 핵심 단발 명령을 마우스로 선택하는 보조 입력

새 기능은 실제 믹싱 흐름을 단순하게 만드는 경우에만 우선한다. 화면 장식이나 고급 기능 때문에 기본 조작의 안정성을 희생하지 않는다.

### 현재 비목표

명시적인 제품 결정 없이는 다음을 우선 구현하지 않는다.

- 4덱 지원
- 스템 분리
- 키 분석, 자동 위상 정렬, 지속 Sync와 같은 고급 비트 기능
- 배틀 DJ 수준의 스크래치 엔진
- Serato, rekordbox, VirtualDJ 등 외부 DJ 앱 제어
- 범용 트랙패드-to-MIDI 브리지
- 스트리밍 서비스나 대규모 음악 라이브러리 관리
- 대형 서드파티 오디오 엔진 도입

기본 스크래치는 현재 제품 기능이지만, 프로용 스크래치 성능은 비목표다.

## 3. 지원 환경과 실행

- macOS 13 이상
- Swift 5.9 이상
- 멀티터치 간접 입력을 제공하는 맥북 내장 트랙패드
- Swift Package Manager
- `swift-atomics` 1.2.0 고정 의존성, 별도 오디오 런타임 설치 없음

빌드와 실행:

```bash
swift build
.build/debug/TrackpadDJ
```

실제 트랙패드 제스처와 오디오 출력은 빌드 성공만으로 검증되지 않는다. 관련 변경에는 맥북 실기기 확인이 별도로 필요하다.

## 4. 현재 입력 계약

기본 활성 덱은 A이고 `Tab`으로 A/B를 전환한다. 트랙패드 전체를 하나의 세로 조그휠로 사용하며 **두 손가락 조그 시작 당시 덱과 모드를 끝까지 고정하는 명시적인 상호작용 상태**를 우선한다.

### 트랙패드

| 입력 | 현재 동작 |
|---|---|
| 한 손가락 | 조그 없음, 재생 유지 |
| 버튼 밖 두 번째 손가락 인식 | 해당 시점의 활성 덱 프리즈, 스크래치 준비 |
| 일반 세로 이동 | 시작한 두 손가락의 속도를 항상 2로 나눠 평균해 시작 덱 스크래치 |
| `Shift`를 누른 상태에서 두 번째 손가락 인식 | 시작 당시 활성 덱의 피치 벤드 준비 |
| `Shift` 시작 후 세로 이동 | 시작 덱에 ±8% 순간 피치 벤드 적용 |
| 수평 이동 | 무시 |
| 세 번째 이후 터치 | 시각화만 수행하고 제어 손가락을 대체하지 않음 |
| 한 손가락만 남거나 시작한 두 손가락 중 하나 종료 | 조그 해제, 기존 재생·정지 상태 유지 |
| 조그 후 한 손가락만 남음 | 커서 잠금 해제, 남은 손가락으로 화면 버튼 클릭 허용. 자동 재잠금 없음 |
| 커서 잠금 해제 상태의 버튼 위 첫 터치 | 해당 터치 시퀀스 전체를 UI 클릭에 예약하고 조그를 시작하지 않음 |

스크래치 목표 속도는 두 손가락 평균 `velocityY × 3.3`으로 계산하고 `-8...8`로 제한한다. 피치 벤드는 평균 `velocityY × 8`을 `-8%...+8%`로 제한하며 유효 재생률은 `1 + (tempoPercent + pitchBendPercent) / 100`이다. 정지한 덱은 피치 벤드로 이동하지 않는다. 두 모드 모두 정지 프레임에는 즉시 목표값을 0으로 만들고, 이벤트가 끊기면 마지막 이동 후 20ms 이상 지난 첫 타이머 틱에서 0으로 만든다. 터치 종료나 취소 시 반드시 해제한다. 터치 도중 `Tab` 또는 `Shift` 상태가 바뀌어도 현재 터치에는 영향을 주지 않는다.

커서가 잠겨 있거나 포인터가 클릭 가능한 컨트롤 밖에 있으면 전체 표면 조그를 유지한다. 버튼 위에서 UI에 예약된 시퀀스는 이동하거나 추가 터치가 들어와도 중간에 조그로 승격하지 않는다.

정지한 손가락이 제어권을 독점하지 않도록 시작한 두 손가락 모두의 이동을 읽는다. AppKit의 resting 터치도 수신하며 매 터치 콜백에서 전체 `.touching` 목록을 대조해 사라진 터치를 해제한다. 추가 손가락은 기존 조그 쌍을 대체하지 않는다.

### 키보드

| 키 | 현재 동작 |
|---|---|
| `Tab` | 활성 덱 A/B 전환 |
| `Q` | 활성 덱 오디오 파일 선택 |
| `Space` | 활성 덱 재생·정지 |
| `C` | 활성 덱 큐, 2초 프리롤 시작점으로 복귀 |
| `S` | 반대 덱의 현재 유효 BPM으로 활성 덱 원샷 템포 매치 |
| `E` / `D` | 활성 덱 볼륨 증가 / 감소 |
| `R` / `F` | 활성 덱 필터 열기 / 닫기 |
| `T` / `G` | 활성 덱 템포 증가 / 감소 |
| `5` | 활성 덱 템포 0% 초기화 |
| `B` | 활성 덱 탭 BPM 보정 |
| `Shift+B` | 탭 보정을 제거하고 자동 BPM 복원 |
| `V` | 활성 덱 프리큐 선택 토글 |
| `↑` / `↓` | 활성 덱 앞 / 뒤로 너지 |
| `←` / `→` | 3단 크로스페이더 게이트 `A ON → A+B ON → B ON` 전환 |
| `M` | Stereo Master / Split Cue 출력 전환 |
| `P` | 커서 잠금·숨김 토글 |
| `Esc` | 활성 조그 취소와 커서 잠금 해제 |

볼륨·필터·템포·너지 키는 누른 당시 덱에 고정되며, 누른 채 `Tab`을 눌러도 다른 덱으로 넘어가지 않는다. 템포는 `-8.0%...+8.0%` 범위에서 키를 누르는 동안 약 60fps로 0.05%씩 바뀐다. Command, Control 또는 Option 조합은 앱 입력으로 소비하지 않고 macOS에 전달한다. 핫큐 후보 키는 실제 덱 저장·점프 기능이 구현될 때까지 매핑하지 않는다.

### 화면 컨트롤

- Deck A/B 헤더, `LOAD`, `CUE`, `PLAY/PAUSE`, `SYNC`, `MON`과 출력 모드 버튼을 클릭할 수 있다.
- 비활성 덱의 버튼은 해당 덱을 먼저 활성화한 뒤 그 덱 명령을 실행한다. 출력 모드는 활성 덱을 바꾸지 않는 전역 명령이다.
- 트랙이 없는 덱의 `CUE`와 `PLAY`, 어느 한 덱이라도 BPM이 없는 경우의 `SYNC`는 비활성화한다.
- 볼륨·필터·템포 다이얼과 크로스페이더는 표시 전용이다. 연속 마우스 조작을 추가해 키보드·조그 계약을 이중화하지 않는다.
- 커서 잠금 중에는 화면 버튼보다 전체 표면 조그를 우선한다.
- 조그에서 한 손가락으로 전환하면 잠금을 해제하고 남은 터치의 UI 클릭을 허용한다. UI에 예약되지 않은 시퀀스에서 두 번째 손가락이 올라오면 대기 중 클릭을 취소하고 조그를 시작한다.

### 입력 변경 규칙

- 기존 키 매핑을 바꿀 때는 충돌, 양손 사용성과 공연 중 접근성을 함께 검토한다.
- 연속 입력에는 클램프, 타임아웃 또는 스무딩이 필요하다.
- 키 반복을 사용하는 제어와 한 번만 실행할 명령을 구분한다.
- 조그 제스처에만 의존하는 기능에는 가능한 경우 키보드 대체 입력을 제공한다. 단, 스크래치와 순간 피치 벤드는 예외로 둘 수 있다.
- 한 제스처가 여러 오디오 파라미터를 동시에 변경하지 않도록 한다.
- 새 제스처를 추가하기 전에 단일 세로 조그 모델을 복잡하게 만들지 않는지 먼저 검토한다.
- 화면에 그리는 컨트롤과 마우스 hit test는 같은 레이아웃 기하를 사용한다.

## 5. 오디오와 UI 동작

### 오디오 경로

각 덱은 `AVAudioSourceNode`에서 오디오를 생성하고 다음 기본 경로로 출력한다.

```text
AVAudioSourceNode → low-pass EQ → master/cue path → output routing → main mixer
```

현재 구현의 주요 특성:

- 파일을 백그라운드에서 디코딩하고 파형과 자동 BeatGrid를 계산한 뒤 `AVAudioPCMBuffer`에 전체 로드한다.
- 늦게 끝난 이전 로드가 최신 요청을 덮지 않도록 덱별 세대 번호를 사용한다.
- 두 덱은 별도 소스, 믹서와 로우패스 EQ를 사용한다.
- 로우패스 범위는 약 200 Hz~20 kHz다.
- 템포는 피치를 함께 바꾸는 Varispeed 방식이며 `-8.0%...+8.0%` 범위다. 피치 벤드는 별도 atomic 값으로 ±8%를 순간 합산한다.
- 크로스페이더는 `A만 ON / 둘 다 ON / B만 ON`의 3단 출력 게이트다. 켜진 덱은 채널 페이더 게인을 그대로 유지하고 선택되지 않은 덱만 뮤트한다.
- 일반 재생률은 `1 + (tempoPercent + pitchBendPercent) / 100`이며, 스크래치 중에는 스크래치 속도가 이를 대체한다.
- 스크래치는 샘플 읽기 위치를 속도와 방향에 따라 이동하는 varispeed 방식이다.
- 일반 변속은 4-point Cubic Hermite 보간, 1배속 초과 스크래치는 사전 계산한 32-tap windowed-sinc 필터로 점진 전환한다. 커널 생성은 렌더러 초기화 때 수행한다.
- 스크래치 속도는 샘플 단위 2ms 시정수 EMA로 연결하고, 정지 부근에는 1ms 시정수 출력 페이드로 DC 출력을 제거한다.
- 로드와 큐 시 곡 앞에 2초 무음 프리롤을 둔다.
- 자동 BeatGrid는 최대 첫 90초의 200Hz 에너지 엔벌로프를 짧게 평활화한 뒤 onset autocorrelation을 사용한다. 80~160 BPM 기본 후보와 가능한 2배 템포 증거를 함께 평가하며 8초 미만 또는 기본 후보 신뢰도 0.35 미만은 실패로 남긴다.
- 탭 BPM은 최근 8회 간격을 사용하고 첫 탭 재생 위치를 첫 박자로 삼는다. 자동 분석값은 보존해 `Shift+B`로 복원한다.
- 원샷 SYNC는 반대 덱의 현재 템포를 반영한 유효 BPM에 맞추되 ±8%를 넘으면 아무 값도 변경하지 않는다. 위상과 재생 위치는 바꾸지 않는다.
- 렌더러는 실제 생성 샘플의 최대 절댓값을 덱별 atomic accumulator에 발행하고, UI 스냅샷은 이를 소비·초기화해 프리페이더 피크를 전달한다.
- 기본 `stereoMaster`에서는 스테레오 마스터만 출력한다.
- `splitCue`에서는 포스트 페이더·포스트 크로스페이더 마스터를 왼쪽 모노로, 선택한 포스트 EQ·프리 페이더 프리큐를 오른쪽 모노로 출력한다.
- 두 프리큐 덱을 동시에 선택하면 각 덱을 0.5 게인으로 합산한다.
- 출력 장치 재구성 실패 시 엔진과 프리큐를 중단하고 오류를 표시한다.

전체 파일 메모리 로드는 프로토타입에 적합하지만 긴 파일이나 대용량 라이브러리에는 제약이 될 수 있다. 스트리밍 방식으로 바꾸는 작업은 별도 설계와 검증 없이 섞어 넣지 않는다.

### UI

- AppKit 기반 단일 반응형 메인 윈도우를 사용한다. 기본 콘텐츠 크기는 1180×720, 최소 크기는 960×620이다.
- 상단에는 Deck A/B 스크롤 파형을 세로로 쌓고 트랙명, 재생 상태, BPM 출처·신뢰도와 남은 시간을 표시한다.
- 하단에는 좌우 덱 콘솔, 중앙 단일 플래터와 3단 크로스페이더 상태를 배치한다.
- 덱별 볼륨, 필터, 템포·벤드, MON 상태와 실제 프리페이더 피크 미터를 표시한다.
- 활성 덱은 파형 레일, 덱 헤더, 플래터와 `JOG A/B` 색을 함께 바꾸고 `SCRATCH/BEND`, 다음 활성 덱과 `CURSOR LOCKED` 상태를 표시한다.
- 핵심 단발 명령은 키 힌트가 있는 화면 버튼으로 제공하며 비활성·hover·pressed·active 상태를 구분한다.
- Split Cue 활성 시 출력 버튼에 `L: MASTER / R: CUE`를 상시 표시한다.
- 자동 또는 탭 BPM이 있으면 출처·신뢰도와 비트 그리드를 표시한다.
- 플레이헤드 표시는 약 30 fps로 갱신하고, 누르고 있는 키 입력은 약 60 fps로 반영한다.
- UI는 `DeckSnapshot`과 `MixerSnapshot`을 한 표시 주기 단위로 적용한다.

UI는 장식보다 현재 덱, 활성 입력, 파라미터와 위험한 상태를 즉시 이해할 수 있게 하는 것을 우선한다.

## 6. 프로젝트 구조와 경계

```text
Sources/TrackpadDJ/
├── App/
│   ├── main.swift
│   └── AppDelegate.swift
├── input/
│   ├── DJAction.swift
│   ├── KeyboardMapping.swift
│   ├── BPMTapState.swift
│   ├── TouchPoint.swift
│   └── TouchSession.swift
├── gestures/
│   └── GestureStateMachine.swift
├── audio/
│   ├── AtomicValue.swift
│   ├── BeatGrid.swift
│   ├── DeckProtocol.swift
│   ├── Deck.swift
│   ├── DeckRealtimeState.swift
│   ├── ScratchResampler.swift
│   ├── DeckSnapshot.swift
│   ├── TrackLoader.swift
│   ├── AudioEngine.swift
│   ├── OutputRouting.swift
│   ├── CrossfaderGate.swift
│   └── CrossfaderState.swift
└── ui/
    ├── CursorLockController.swift
    ├── PerformanceLayout.swift
    ├── PerformanceConsoleRenderer.swift
    ├── TouchLabView.swift
    └── TouchLabViewController.swift
```

모듈 책임:

- `App/`: 애플리케이션 진입점과 윈도우 수명주기
- `input/`: 키보드 상태, BPM 탭, 원시 터치와 의미 단위 `DJAction`
- `gestures/`: 전체 표면 단일 조그의 결정론적 터치 상태 머신
- `audio/`: 비동기 로드, 덱 재생, atomic 실시간 상태, 믹싱, 라우팅과 UI 스냅샷
- `ui/`: 반응형 레이아웃과 hit test, 입력 이벤트 수신, 불변 상태 기반 퍼포먼스 콘솔·파형 렌더링과 오디오 콜백 연결
- `config/`: 레이아웃, 감도와 키 매핑이 충분히 커질 때 도입할 수 있는 예정 경계

경계 규칙:

- 원시 입력 처리와 오디오 파라미터 적용을 직접 결합하지 않는다.
- UI는 의미 단위 콜백으로 명령을 전달하고 `DeckSnapshot`과 `MixerSnapshot`으로 오디오 상태를 읽는다.
- `TouchLabView`는 입력·수명주기와 렌더 상태 조립을 담당하고, `PerformanceConsoleRenderer`는 한 표시 주기의 불변 상태만 소비한다.
- 제스처 판정 규칙을 오디오 렌더 콜백에 넣지 않는다.
- 오디오 렌더 경로에는 블로킹 I/O, 메모리 할당, UI 접근과 로그 출력을 추가하지 않는다.
- 공유 오디오 상태를 추가할 때는 UI 스레드와 실시간 오디오 스레드 간 동시성을 명시적으로 검토한다.
- 실제 중복이 생기기 전에는 추상화나 설정 계층을 미리 만들지 않는다.

## 7. 현재 구현 상태

아래 `[x]`는 현재 소스에 구현이 존재하고 로컬 빌드가 가능한 항목을 뜻한다. 실제 트랙패드 조작성과 오디오 품질까지 PASS했다는 의미는 아니다.

### 완료된 기반

- [x] 전체 표면 터치 포인트 시각화와 활성 덱 외곽선
- [x] 1180×720 기본·960×620 최소 크기의 반응형 퍼포먼스 콘솔
- [x] 세로로 쌓인 2덱 파형과 중앙 단일 플래터
- [x] 덱 선택·LOAD·CUE·PLAY/PAUSE·SYNC·MON·출력 모드 화면 버튼
- [x] 비활성 덱 버튼의 덱 우선 선택과 화면 컨트롤/조그 터치 시퀀스 중재
- [x] 실제 렌더 신호 기반 덱별 프리페이더 피크 미터
- [x] 두 오디오 파일 로드와 2덱 재생
- [x] 재생·정지와 큐
- [x] 키보드 너지
- [x] 덱별 볼륨
- [x] 덱별 로우패스 필터
- [x] 키보드 3단 크로스페이더 전환
- [x] 켜진 덱의 볼륨을 바꾸지 않는 3단 ON/OFF 크로스페이더 게이트
- [x] 덱별 ±8% 수동 Varispeed 템포
- [x] 전체 트랙패드 프리즈와 양방향 기본 스크래치
- [x] `Shift` 시작 ±8% 피치 벤드와 정지 덱 불변
- [x] 두 손가락 고정 평균, 시작 덱·모드 고정, 정지 프레임 즉시 반응과 20ms 안전 타임아웃
- [x] 버퍼 크기 독립 스크래치 반응, 정지 페이드와 고속 앨리어싱 감소 필터
- [x] 조그 쌍 양쪽 이동 처리와 전체 터치 목록 기반 종료 누락 복구
- [x] 조그 후 한 손가락 UI 클릭 전환과 커서 잠금 자동 해제
- [x] 스크래치 곡 끝 경계에서 재생 상태 보존
- [x] 기본 활성 덱 A, `Tab` 전환과 공용 키 배열
- [x] 연속 키의 시작 덱 고정과 전역 키 독립 처리
- [x] 스크롤 파형, 플레이헤드와 SCRATCH/BEND 피드백
- [x] 자동 BPM·첫 박자 분석, 탭 보정·복원과 비트 그리드
- [x] 반대 덱 현재 템포를 반영한 원샷 BPM 매치
- [x] 비동기 트랙 디코딩·파형·BeatGrid 계산과 최신 요청 우선 설치
- [x] atomic 렌더 상태와 버퍼 경계 탐색 명령
- [x] Stereo Master / 왼쪽 Master·오른쪽 Cue Split Cue
- [x] `DeckSnapshot` 기반 UI 상태 갱신
- [x] 선택형 커서 잠금과 포커스·파일 선택·종료 복구
- [x] 입력 trace, 렌더 상태, 로드, 크로스페이더와 채널 라우팅 자동 테스트
- [x] AVFoundation과 `swift-atomics` 기반 Swift Package 빌드

### 부분 구현 또는 미구현

- [~] BPM: 자동 분석, 탭 보정과 원샷 템포 매치를 제공하고 현재 힙합 2곡의 BPM 표시는 확인했지만 다양한 장르 정확도와 첫 박자 위상은 미검증
- [ ] 핫큐: 미작동 키와 마커를 제거했으며 덱 저장·점프 계약부터 구현해야 함
- [ ] 루프
- [ ] 트랙패드 또는 키보드 중심의 자체 트랙 브라우저
- [ ] 조그 감도와 정지 타임아웃 보정 UI
- [ ] 실제 공연 흐름을 기준으로 한 장시간 실기기 테스트
- [ ] 실제 헤드폰에서 Split Cue 분리와 장치 연결·해제 복구 확인

문서의 체크 표시는 현재 소스와 검증 결과를 반영해야 한다. 선언된 콜백, UI 요소 또는 과거 완료 기록만으로 기능을 완료 처리하지 않는다.

## 8. 다음 우선순위

1. 맥북 실기기에서 두 손가락 조그 → 한 손가락 남기기 → 커서 복구·화면 버튼 클릭 → 두 손가락 조그 재시작 흐름과 반복 조작 시 재생 복귀를 확인한다.
2. `Tab` 전환, 전체 표면 스크래치와 `Shift` 피치 벤드의 방향·감도·복귀를 확인한다.
3. 960×620부터 큰 창까지 크기를 바꾸며 컨트롤 겹침, 잘림과 클릭 hit target을 확인한다.
4. `P`, `Esc`, 파일 선택, 포커스 상실과 창 종료에서 커서가 항상 복구되는지 확인한다.
5. 더 다양한 장르에서 자동 BPM·첫 박자 정확도, 탭 보정·복원과 원샷 SYNC 후 수동 위상 조정을 확인한다.
6. 헤드폰으로 왼쪽 Master·오른쪽 Cue 분리, 프리큐 토글 시 마스터 불변과 장치 연결·해제 복구를 확인한다.
7. 30분 연속 믹싱으로 클릭, 멈춤, 키 고정과 상태 누적 여부를 확인한다.
8. 실기기에서 20ms 타임아웃·2ms 속도 시정수·1ms 출력 페이드, 두 손가락 평균 감도와 고속 필터 음색을 비교 청취한다.
9. 핫큐는 덱 계층의 저장·점프 계약과 범위 처리를 먼저 완성한 뒤 UI에 연결한다.
10. 루프는 기본 믹싱 안정성 및 핫큐 검증 이후에 추가한다.

우선순위가 바뀌면 이 목록과 `현재 구현 상태`를 같은 변경에서 갱신한다.

## 9. 개발 규칙

- 변경 전 `git status`, 관련 소스와 현재 문서를 확인한다.
- 작고 독립적이며 되돌리기 쉬운 변경을 선호한다.
- 요청과 무관한 리팩터링, 이름 변경과 포맷 정리를 섞지 않는다.
- 프레임워크 동작이 불확실하면 Apple 공식 문서를 먼저 확인한다.
- AppKit, AVFoundation과 macOS 터치 입력에 대해 추측으로 API를 작성하지 않는다.
- 읽기 쉬운 직접적인 코드를 우선하고, 비직관적인 제스처·오디오 처리에 짧은 설명을 남긴다.
- 큰 서드파티 의존성을 추가하기 전에 표준 프레임워크로 해결 가능한지 검토한다.
- 의존성 추가, 입력 계약 변경 또는 오디오 엔진 교체는 구현 전에 영향 범위를 기록한다.
- 기존 사용자의 작업과 무관한 파일은 수정, 삭제, 포맷하거나 스테이징하지 않는다.

## 10. 검증 규칙

작업 완료 판정은 변경 범위에 맞춰 다음 단계를 구분한다.

1. **정적 확인:** 변경한 타입, 콜백과 모듈 경계가 일관적인지 검토한다.
2. **빌드 확인:** 최소한 `swift build`를 실행한다.
3. **결정론적 확인:** 가능한 경우 단위 테스트나 재생 가능한 입력 로그로 경계값을 검증한다.
4. **실기기 확인:** 트랙패드 제스처, 오디오 출력, 레이턴시와 클릭 노이즈를 실제 맥북에서 확인한다.
5. **전체 흐름 확인:** 두 곡 로드 → 큐 → 재생 → 템포·필터·볼륨 → 크로스페이드 → Split Cue를 수행한다.

빌드 성공은 실제 오디오나 제스처 동작의 증거가 아니다. 실기기 검증을 하지 못했다면 결과를 `미검증`으로 명시한다.

특히 다음 회귀를 확인한다.

- 한 덱을 조작할 때 다른 덱 값이 변하지 않는가
- 터치와 연속 키가 시작 당시 덱에 고정되고 `Tab` 이후 다른 덱으로 넘어가지 않는가
- 커서 잠금 해제 상태의 버튼 위 첫 터치는 전체 시퀀스 동안 조그를 시작하지 않는가
- 버튼 밖 또는 커서 잠금 상태의 터치는 전체 표면 조그로 동작하는가
- 비활성 덱의 화면 버튼이 덱 선택을 먼저 적용하고 명령을 올바른 덱에 보내는가
- 최소·기본·큰 창에서 컨트롤이 범위를 벗어나거나 서로 겹치지 않고 그린 영역과 hit test가 일치하는가
- 일반 터치와 `Shift` 시작 터치의 모드가 종료까지 고정되는가
- 수평 이동이 오디오 파라미터를 변경하지 않는가
- 터치 종료·취소 후 스크래치나 피치 벤드가 남지 않는가
- 피치 벤드가 정지한 덱의 재생 위치를 움직이지 않는가
- 볼륨과 필터 값이 허용 범위를 벗어나지 않는가
- 크로스페이더 양 끝에서 선택되지 않은 덱만 뮤트되고 중앙에서 두 덱의 채널 페이더 게인이 그대로 유지되는가
- 스크래치를 끝내면 설정된 템포와 현재 벤드의 유효 재생률로 복귀하는가
- 역방향 스크래치와 프리롤·트랙 끝 경계에서 범위를 벗어나지 않는가
- 트랙 재로드 후 오디오 그래프, 자동 BeatGrid와 UI가 새 파일 상태를 반영하고 탭·템포·벤드를 초기화하는가
- 덱별 피크 미터가 실제 다중 채널 렌더 최대값을 반영하고 스냅샷 소비 후 초기화되는가
- 원샷 SYNC가 상대 덱 템포를 반영하고 BPM 누락·±8% 초과 시 값을 바꾸지 않는가
- 커서 잠금과 해제가 멱등적이고 `hide`/`unhide` 호출이 균형을 이루는가
- Split Cue에서 마스터는 왼쪽, 선택한 프리큐는 오른쪽에만 존재하는가
- 프리큐를 켜거나 끌 때 마스터 게인이 변하지 않는가
- Command, Control과 Option 조합을 앱 키 매핑이 가로채지 않는가

테스트를 추가할 때는 다음을 선호한다.

- 전체 표면 첫 터치, 시작 덱·모드 고정과 수평 무시 테스트
- 활성 덱 전환, 연속 키 덱 고정, 키 반복과 단발 명령 테스트
- 스크래치·피치 벤드 속도, 클램프, 20ms 정지, 버퍼 크기 독립성·무음·앨리어싱과 프리롤 경계 테스트
- 크로스페이더·볼륨·필터 계산 테스트
- 합성 클릭·밀집 에너지·기본/더블 템포 BeatGrid, 탭 복원과 원샷 SYNC 테스트
- atomic 탐색·벤드 명령, 커서 시스템 호출과 Split Cue 채널 라우팅 테스트
- 반응형 레이아웃, 컨트롤 hit test·활성 정책, 클릭 명령 순서와 offscreen 렌더 테스트
- 버튼 위 첫 터치와 커서 잠금에 따른 UI/조그 라우팅 정책 테스트
- 입력 이벤트를 재생할 수 있는 작은 로그 기반 테스트

## 11. 문서와 인수인계

의미 있는 작업을 마칠 때 다음 내용을 사용자에게 보고한다.

- 무엇을 변경했는가
- 무엇을 어떤 명령으로 확인했는가
- 실제 트랙패드와 오디오까지 확인했는가
- 남은 미구현 또는 위험 요소는 무엇인가
- 다음으로 할 가장 작은 작업은 무엇인가
- Git 변경 상태는 어떠한가

제품 목표, 입력 매핑, 아키텍처, 구현 완료 여부 또는 다음 우선순위가 달라지면 이 `AGENTS.md`를 갱신한다. 사용자용 실행법이나 확정된 기능이 바뀌면 `README.md`도 함께 갱신한다.

커밋, 푸시, PR, 릴리스와 배포는 각각 별도 작업이다. 사용자가 명시적으로 요청하지 않았다면 문서나 코드 변경을 자동으로 스테이징, 커밋 또는 게시하지 않는다.

## 12. MVP 완료 기준

다음 전체 흐름을 맥북의 트랙패드와 키보드만으로 반복 수행할 수 있을 때 MVP가 성공한 것으로 본다.

1. 서로 다른 두 트랙을 Deck A와 Deck B에 로드한다.
2. 각 덱을 재생·정지하고 큐 위치로 되돌린다.
3. `Tab`으로 활성 덱을 고르며 볼륨과 필터로 다음 곡을 준비한다.
4. 필요하면 너지, 전체 표면 스크래치 또는 순간 피치 벤드로 위치와 위상을 조절한다.
5. 자동 BPM·원샷 매치를 참고하되 수동 템포와 크로스페이더로 한 곡에서 다른 곡으로 자연스럽게 전환한다.
6. 필요하면 Split Cue로 다음 덱을 오른쪽 채널에서 미리 듣되 왼쪽 마스터를 유지한다.

완성형 DJ 하드웨어처럼 느껴질 필요는 없다. **별도 컨트롤러 없이 트랙패드와 키보드 기반의 간단한 2덱 디제잉이 실제로 가능함을 입증하면 된다.**
