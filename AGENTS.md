# Trackpad DJ Project Guide

이 문서는 Trackpad DJ 저장소의 제품 방향, 현재 구현 상태, 입력 규칙, 아키텍처, 개발 우선순위와 작업 규칙을 관리하는 기준 문서다.

에이전트는 작업을 시작하기 전에 이 문서와 변경 대상 소스를 읽어야 한다. 문서의 목표와 현재 소스가 다르면 추측으로 맞추지 말고 차이를 명시한다. 의미 있는 기능 변경으로 프로젝트 상태가 달라졌다면 이 문서의 `현재 구현 상태`와 `다음 우선순위`도 함께 갱신한다.

## 0. 사용자 응답 형식

- 사용자에게 보이는 자연어 `commentary`와 `final` 응답은 첫 줄에 `◆ CODEX ━━━━━━━━━━━━━━━━━━━━━`를 단독으로 표시한다.
- 구분선 다음에는 빈 줄을 하나 둔 뒤 본문을 작성한다.
- 코드 블록, 명령어, 파일 내용, 인용문과 사용자가 요청한 정확한 출력 형식 안에는 구분선을 삽입하지 않는다.
- 사용자가 헤더 생략이나 정확한 기계 판독 형식을 명시적으로 요구하면 해당 요청을 우선한다.
- 별도 요청이 없다면 사용자 응답과 인수인계 문서는 한국어로 작성한다.

## 1. 프로젝트 정의

Trackpad DJ는 **맥북의 내장 트랙패드와 키보드를 함께 사용해 실제로 간단한 디제잉을 할 수 있는지 검증하는 macOS 우선 실험 앱**이다.

핵심 목표는 다음과 같다.

- 두 곡을 각각 Deck A와 Deck B에 로드한다.
- 재생, 정지, 큐와 기본적인 위치 이동을 수행한다.
- 트랙패드로 볼륨, 필터, 스크래치와 크로스페이더를 연속 제어한다.
- 키보드로 트랙 로드, 트랜스포트와 보조 제어를 빠르고 확실하게 수행한다.
- 외부 DJ 컨트롤러 없이 두 곡을 자연스럽게 전환한다.

이 프로젝트는 완성형 프로 DJ 장비를 복제하는 제품이 아니다. **트랙패드와 키보드만으로 플레이 가능한 2덱 믹싱 경험**을 만드는 HCI + 오디오 인터랙션 프로젝트다.

## 2. 제품 원칙

우선순위는 다음 순서를 따른다.

1. 두 덱의 안정적인 재생과 자연스러운 곡 전환
2. 공연 중 헷갈리지 않는 트랙패드 존과 키보드 매핑
3. 빠른 로드, 재생·정지, 큐와 위치 조절
4. 덱별 볼륨, 필터와 크로스페이더
5. 기본 스크래치의 반응성과 음질
6. BPM, 핫큐, 루프와 같은 확장 기능

트랙패드와 키보드의 역할은 다음과 같이 나눈다.

- **트랙패드:** 볼륨, 필터, 스크래치, 크로스페이더처럼 연속적인 퍼포먼스 제어
- **키보드:** 로드, 재생·정지, 큐, 탭 BPM처럼 즉각적이고 명확한 명령과 보조 제어

새 기능은 실제 믹싱 흐름을 단순하게 만드는 경우에만 우선한다. 화면 장식이나 고급 기능 때문에 기본 조작의 안정성을 희생하지 않는다.

### 현재 비목표

명시적인 제품 결정 없이는 다음을 우선 구현하지 않는다.

- 4덱 지원
- 스템 분리
- 고급 비트 분석이나 자동 비트 싱크
- 배틀 DJ 수준의 스크래치 엔진
- Serato, rekordbox, VirtualDJ 등 외부 DJ 앱 제어
- 범용 트랙패드-to-MIDI 브리지
- 스트리밍 서비스나 대규모 음악 라이브러리 관리
- 대형 서드파티 오디오 엔진 도입

기본 스크래치는 현재 제품 기능이지만, 프로용 스크래치 성능은 비목표다.

## 3. 지원 환경과 실행

- macOS 13 이상
- Swift 5.9 이상
- 멀티터치 간접 입력을 제공하는 맥북 내장 트랙패드
- Swift Package Manager
- `swift-atomics` 1.2.0 고정 의존성, 별도 오디오 런타임 설치 없음

빌드와 실행:

```bash
swift build
.build/debug/TrackpadDJ
```

실제 트랙패드 제스처와 오디오 출력은 빌드 성공만으로 검증되지 않는다. 관련 변경에는 맥북 실기기 확인이 별도로 필요하다.

## 4. 현재 입력 계약

트랙패드는 정규화 좌표 기준으로 상단, Deck A, Deck B, 하단의 고정 영역으로 나뉜다. 자유형 제스처 추측보다 **존 기반 입력과 명시적인 상호작용 상태**를 우선한다.

### 트랙패드

| 영역 | 입력 | 현재 동작 |
|---|---|---|
| 상단 왼쪽 | 한 손가락 수직 이동 | Deck A 볼륨 |
| 상단 오른쪽 | 한 손가락 수직 이동 | Deck B 볼륨 |
| Deck A/B | 터치 시작 또는 정지 | 해당 덱 프리즈, 스크래치 준비 |
| Deck A/B | 수직 이동 | 이동 속도와 방향에 비례한 스크래치 |
| Deck A/B | 수평 이동 | 해당 덱 로우패스 필터 |
| 하단 | 한 손가락 수평 이동 | 연속 크로스페이더 |

덱 영역에서는 우세 축만 적용해 대각선 입력이 스크래치와 필터를 동시에 발동하지 않도록 한다. 누적 이동 0.006 이상, 우세 축 비율 1.25를 기본값으로 사용한다. 스크래치 목표 속도는 `위치 변화 / 시간 변화 × 3.3`으로 계산하고 `-8...8`로 제한하며, 마지막 이동 후 50ms가 지나면 0으로 만든다. 제스처 시작 존과 존별 첫 제어 터치는 종료까지 고정하고, 터치 종료나 취소 시 스크래치 상태를 반드시 해제한다.

### 키보드

| 키 | 현재 동작 |
|---|---|
| `Q` / `W` | Deck A / B 오디오 파일 선택 |
| `A` / `S` | Deck A / B 재생·정지 |
| `Z` / `X` | Deck A / B 큐, 2초 프리롤 시작점으로 복귀 |
| `E` / `D` | Deck A 볼륨 증가 / 감소 |
| `R` / `F` | Deck B 볼륨 증가 / 감소 |
| `T` / `G` | Deck A 필터 열기 / 닫기 |
| `Y` / `H` | Deck B 필터 열기 / 닫기 |
| `↑` / `↓` | Deck A 앞 / 뒤로 너지 |
| `I` / `K` | Deck B 앞 / 뒤로 너지 |
| `←` / `→` | 크로스페이더 `A → A+B → B` 전환 |
| `B` / `N` | Deck A / B 탭 BPM |
| `U` / `J` | Deck A 템포 증가 / 감소 |
| `O` / `L` | Deck B 템포 증가 / 감소 |
| `5` / `6` | Deck A / B 템포를 0%로 초기화 |
| `C` / `V` | Deck A / B 프리큐 선택 토글 |
| `M` | Stereo Master / Split Cue 출력 전환 |

템포는 `-8.0%...+8.0%` 범위에서 키를 누르는 동안 약 60fps로 0.05%씩 바뀐다. Command, Control 또는 Option 조합은 앱 입력으로 소비하지 않고 macOS에 전달한다. 핫큐 후보였던 Deck A의 `1`~`4`와 Deck B의 `7`~`0`은 실제 덱 저장·점프 기능이 구현될 때까지 매핑하지 않는다.

### 입력 변경 규칙

- 기존 키 매핑을 바꿀 때는 충돌, 양손 사용성과 공연 중 접근성을 함께 검토한다.
- 연속 입력에는 클램프, 데드존 또는 스무딩이 필요하다.
- 키 반복을 사용하는 제어와 한 번만 실행할 명령을 구분한다.
- 트랙패드 제스처에만 의존하는 기능에는 가능한 경우 키보드 대체 입력을 제공한다. 단, 스크래치는 예외로 둘 수 있다.
- 한 제스처가 여러 오디오 파라미터를 동시에 변경하지 않도록 한다.
- 새 제스처를 추가하기 전에 현재 존 모델로 표현할 수 있는지 먼저 검토한다.

## 5. 오디오와 UI 동작

### 오디오 경로

각 덱은 `AVAudioSourceNode`에서 오디오를 생성하고 다음 기본 경로로 출력한다.

```text
AVAudioSourceNode → low-pass EQ → master/cue path → output routing → main mixer
```

현재 구현의 주요 특성:

- 파일을 백그라운드에서 디코딩하고 파형을 계산한 뒤 `AVAudioPCMBuffer`에 전체 로드한다.
- 늦게 끝난 이전 로드가 최신 요청을 덮지 않도록 덱별 세대 번호를 사용한다.
- 두 덱은 별도 소스, 믹서와 로우패스 EQ를 사용한다.
- 로우패스 범위는 약 200 Hz~20 kHz다.
- 템포는 피치를 함께 바꾸는 Varispeed 방식이며 `-8.0%...+8.0%` 범위다.
- 크로스페이더는 Equal-power 커브이며 중앙에서 두 덱이 각각 약 0.707 게인이다.
- 스크래치는 샘플 읽기 위치를 속도와 방향에 따라 이동하는 varispeed 방식이다.
- 변속 재생에는 4-point Cubic Hermite 보간을 사용한다.
- 스크래치 속도에는 EMA 스무딩을 적용한다.
- 로드와 큐 시 곡 앞에 2초 무음 프리롤을 둔다.
- 기본 `stereoMaster`에서는 스테레오 마스터만 출력한다.
- `splitCue`에서는 포스트 페이더·포스트 크로스페이더 마스터를 왼쪽 모노로, 선택한 포스트 EQ·프리 페이더 프리큐를 오른쪽 모노로 출력한다.
- 두 프리큐 덱을 동시에 선택하면 각 덱을 0.5 게인으로 합산한다.
- 출력 장치 재구성 실패 시 엔진과 프리큐를 중단하고 오류를 표시한다.

전체 파일 메모리 로드는 프로토타입에 적합하지만 긴 파일이나 대용량 라이브러리에는 제약이 될 수 있다. 스트리밍 방식으로 바꾸는 작업은 별도 설계와 검증 없이 섞어 넣지 않는다.

### UI

- AppKit 기반 단일 메인 윈도우를 사용한다.
- 두 덱의 트랙명, 재생 상태, 남은 시간과 스크롤 파형을 표시한다.
- 중앙 플레이헤드와 스크래치 방향·속도를 표시한다.
- 덱별 볼륨, 필터, 템포와 MON 상태, 크로스페이더 위치를 표시한다.
- Split Cue 활성 시 `L: MASTER / R: CUE` 경고를 상시 표시한다.
- 탭 BPM이 계산되면 BPM과 비트 그리드를 표시한다.
- 플레이헤드 표시는 약 30 fps로 갱신하고, 누르고 있는 키 입력은 약 60 fps로 반영한다.
- UI는 `DeckSnapshot`과 `MixerSnapshot`을 한 표시 주기 단위로 적용한다.

UI는 장식보다 현재 덱, 활성 입력, 파라미터와 위험한 상태를 즉시 이해할 수 있게 하는 것을 우선한다.

## 6. 프로젝트 구조와 경계

```text
Sources/TrackpadDJ/
├── App/
│   ├── main.swift
│   └── AppDelegate.swift
├── input/
│   ├── DJAction.swift
│   ├── KeyboardMapping.swift
│   ├── BPMTapState.swift
│   ├── TouchPoint.swift
│   └── TouchSession.swift
├── gestures/
│   ├── GestureStateMachine.swift
│   └── Zone.swift
├── audio/
│   ├── AtomicValue.swift
│   ├── DeckProtocol.swift
│   ├── Deck.swift
│   ├── DeckRealtimeState.swift
│   ├── DeckSnapshot.swift
│   ├── TrackLoader.swift
│   ├── AudioEngine.swift
│   ├── OutputRouting.swift
│   ├── CrossfaderCurve.swift
│   └── CrossfaderState.swift
└── ui/
    ├── TouchLabView.swift
    └── TouchLabViewController.swift
```

모듈 책임:

- `App/`: 애플리케이션 진입점과 윈도우 수명주기
- `input/`: 키보드 상태, BPM 탭, 원시 터치와 의미 단위 `DJAction`
- `gestures/`: 트랙패드 존과 결정론적 제스처 상태 머신
- `audio/`: 비동기 로드, 덱 재생, atomic 실시간 상태, 믹싱, 라우팅과 UI 스냅샷
- `ui/`: 입력 이벤트 수신, HUD, 파형과 상태 렌더링, 오디오 콜백 연결
- `config/`: 레이아웃, 감도와 키 매핑이 충분히 커질 때 도입할 수 있는 예정 경계

경계 규칙:

- 원시 입력 처리와 오디오 파라미터 적용을 직접 결합하지 않는다.
- UI는 의미 단위 콜백으로 명령을 전달하고 `DeckSnapshot`과 `MixerSnapshot`으로 오디오 상태를 읽는다.
- 제스처 판정 규칙을 오디오 렌더 콜백에 넣지 않는다.
- 오디오 렌더 경로에는 블로킹 I/O, 메모리 할당, UI 접근과 로그 출력을 추가하지 않는다.
- 공유 오디오 상태를 추가할 때는 UI 스레드와 실시간 오디오 스레드 간 동시성을 명시적으로 검토한다.
- 실제 중복이 생기기 전에는 추상화나 설정 계층을 미리 만들지 않는다.

## 7. 현재 구현 상태

아래 `[x]`는 현재 소스에 구현이 존재하고 로컬 빌드가 가능한 항목을 뜻한다. 실제 트랙패드 조작성과 오디오 품질까지 PASS했다는 의미는 아니다.

### 완료된 기반

- [x] Touch Lab에서 터치 포인트와 존 시각화
- [x] 두 오디오 파일 로드와 2덱 재생
- [x] 재생·정지와 큐
- [x] 키보드 너지
- [x] 덱별 볼륨
- [x] 덱별 로우패스 필터
- [x] 연속 트랙패드 크로스페이더와 키보드 3단 전환
- [x] Equal-power 크로스페이더
- [x] 덱별 ±8% 수동 Varispeed 템포
- [x] 트랙패드 프리즈와 양방향 기본 스크래치
- [x] 시작 존·첫 터치 고정과 우세 축 제스처 상태 머신
- [x] 스크롤 파형, 플레이헤드와 스크래치 피드백
- [x] 탭 BPM 계산과 비트 그리드 표시
- [x] 비동기 트랙 디코딩·파형 계산과 최신 요청 우선 설치
- [x] atomic 렌더 상태와 버퍼 경계 탐색 명령
- [x] Stereo Master / 왼쪽 Master·오른쪽 Cue Split Cue
- [x] `DeckSnapshot` 기반 UI 상태 갱신
- [x] 입력 trace, 렌더 상태, 로드, 크로스페이더와 채널 라우팅 자동 테스트
- [x] AVFoundation과 `swift-atomics` 기반 Swift Package 빌드

### 부분 구현 또는 미구현

- [~] BPM: 수동 탭과 표시만 제공하며 자동 분석, 저장과 싱크는 없음
- [ ] 핫큐: 미작동 키와 마커를 제거했으며 덱 저장·점프 계약부터 구현해야 함
- [ ] 루프
- [ ] 트랙패드 또는 키보드 중심의 자체 트랙 브라우저
- [ ] 감도, 데드존과 존 경계 보정 UI
- [ ] 실제 공연 흐름을 기준으로 한 장시간 실기기 테스트
- [ ] 실제 헤드폰에서 Split Cue 분리와 장치 연결·해제 복구 확인

문서의 체크 표시는 현재 소스와 검증 결과를 반영해야 한다. 선언된 콜백, UI 요소 또는 과거 완료 기록만으로 기능을 완료 처리하지 않는다.

## 8. 다음 우선순위

1. 맥북 실기기에서 두 곡 로드부터 템포 조정과 전환까지 전체 흐름을 반복 검증한다.
2. 헤드폰으로 왼쪽 Master·오른쪽 Cue 분리, 프리큐 토글 시 마스터 불변과 장치 연결·해제 복구를 확인한다.
3. 30분 연속 믹싱으로 클릭, 멈춤, 키 고정과 상태 누적 여부를 확인한다.
4. 실기기 결과에 따라 스크래치·필터 우세 축, 감도, 데드존과 존 경계를 튜닝한다.
5. 키보드만으로도 파일 선택과 기본 트랜스포트를 빠르게 수행할 수 있게 로드 흐름을 개선한다.
6. 핫큐는 덱 계층의 저장·점프 계약과 범위 처리를 먼저 완성한 뒤 UI에 연결한다.
7. 루프는 기본 믹싱 안정성 및 핫큐 검증 이후에 추가한다.
8. 자동 BPM 분석이나 비트 싱크는 별도의 제품 결정 전까지 확장하지 않는다.

우선순위가 바뀌면 이 목록과 `현재 구현 상태`를 같은 변경에서 갱신한다.

## 9. 개발 규칙

- 변경 전 `git status`, 관련 소스와 현재 문서를 확인한다.
- 작고 독립적이며 되돌리기 쉬운 변경을 선호한다.
- 요청과 무관한 리팩터링, 이름 변경과 포맷 정리를 섞지 않는다.
- 프레임워크 동작이 불확실하면 Apple 공식 문서를 먼저 확인한다.
- AppKit, AVFoundation과 macOS 터치 입력에 대해 추측으로 API를 작성하지 않는다.
- 읽기 쉬운 직접적인 코드를 우선하고, 비직관적인 제스처·오디오 처리에 짧은 설명을 남긴다.
- 큰 서드파티 의존성을 추가하기 전에 표준 프레임워크로 해결 가능한지 검토한다.
- 의존성 추가, 입력 계약 변경 또는 오디오 엔진 교체는 구현 전에 영향 범위를 기록한다.
- 기존 사용자의 작업과 무관한 파일은 수정, 삭제, 포맷하거나 스테이징하지 않는다.

## 10. 검증 규칙

작업 완료 판정은 변경 범위에 맞춰 다음 단계를 구분한다.

1. **정적 확인:** 변경한 타입, 콜백과 모듈 경계가 일관적인지 검토한다.
2. **빌드 확인:** 최소한 `swift build`를 실행한다.
3. **결정론적 확인:** 가능한 경우 단위 테스트나 재생 가능한 입력 로그로 경계값을 검증한다.
4. **실기기 확인:** 트랙패드 제스처, 오디오 출력, 레이턴시와 클릭 노이즈를 실제 맥북에서 확인한다.
5. **전체 흐름 확인:** 두 곡 로드 → 큐 → 재생 → 템포·필터·볼륨 → 크로스페이드 → Split Cue를 수행한다.

빌드 성공은 실제 오디오나 제스처 동작의 증거가 아니다. 실기기 검증을 하지 못했다면 결과를 `미검증`으로 명시한다.

특히 다음 회귀를 확인한다.

- 한 덱을 조작할 때 다른 덱 값이 변하지 않는가
- 대각선 이동이 스크래치와 필터를 동시에 발동하지 않는가
- 터치 종료·취소 후 덱이 프리즈 상태에 남지 않는가
- 볼륨과 필터 값이 허용 범위를 벗어나지 않는가
- 크로스페이더 양 끝과 중앙에서 예상한 게인이 적용되는가
- 스크래치를 끝내면 설정된 템포 재생률로 복귀하는가
- 역방향 스크래치와 프리롤·트랙 끝 경계에서 범위를 벗어나지 않는가
- 트랙 재로드 후 오디오 그래프와 UI가 새 파일 상태를 반영하는가
- Split Cue에서 마스터는 왼쪽, 선택한 프리큐는 오른쪽에만 존재하는가
- 프리큐를 켜거나 끌 때 마스터 게인이 변하지 않는가
- Command, Control과 Option 조합을 앱 키 매핑이 가로채지 않는가

테스트를 추가할 때는 다음을 선호한다.

- 존 경계와 우세 축 판정 테스트
- 키 반복과 단발 명령 테스트
- 스크래치 속도, 클램프와 프리롤 경계 테스트
- 크로스페이더·볼륨·필터 계산 테스트
- atomic 탐색 명령과 Split Cue 채널 라우팅 테스트
- 입력 이벤트를 재생할 수 있는 작은 로그 기반 테스트

## 11. 문서와 인수인계

의미 있는 작업을 마칠 때 다음 내용을 사용자에게 보고한다.

- 무엇을 변경했는가
- 무엇을 어떤 명령으로 확인했는가
- 실제 트랙패드와 오디오까지 확인했는가
- 남은 미구현 또는 위험 요소는 무엇인가
- 다음으로 할 가장 작은 작업은 무엇인가
- Git 변경 상태는 어떠한가

제품 목표, 입력 매핑, 아키텍처, 구현 완료 여부 또는 다음 우선순위가 달라지면 이 `AGENTS.md`를 갱신한다. 사용자용 실행법이나 확정된 기능이 바뀌면 `README.md`도 함께 갱신한다.

커밋, 푸시, PR, 릴리스와 배포는 각각 별도 작업이다. 사용자가 명시적으로 요청하지 않았다면 문서나 코드 변경을 자동으로 스테이징, 커밋 또는 게시하지 않는다.

## 12. MVP 완료 기준

다음 전체 흐름을 맥북의 트랙패드와 키보드만으로 반복 수행할 수 있을 때 MVP가 성공한 것으로 본다.

1. 서로 다른 두 트랙을 Deck A와 Deck B에 로드한다.
2. 각 덱을 재생·정지하고 큐 위치로 되돌린다.
3. 볼륨과 필터로 다음 곡을 준비한다.
4. 필요하면 너지 또는 기본 스크래치로 위치를 조절한다.
5. 수동 템포와 크로스페이더를 이용해 한 곡에서 다른 곡으로 자연스럽게 전환한다.
6. 필요하면 Split Cue로 다음 덱을 오른쪽 채널에서 미리 듣되 왼쪽 마스터를 유지한다.

완성형 DJ 하드웨어처럼 느껴질 필요는 없다. **별도 컨트롤러 없이 트랙패드와 키보드 기반의 간단한 2덱 디제잉이 실제로 가능함을 입증하면 된다.**
